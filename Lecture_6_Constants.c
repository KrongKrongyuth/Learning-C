#include <stdio.h>

int main(){

    // cinstant = fixed value that cannot be altered by the program during its execution.

    const float PI = 3.14159; // We cannot edit the constant variable.

    // PI = 555; // Error

    printf("\n%f\n",PI);

    return 0;
}